function soma(array){
    let soma = 0;
    for(let i = 0; i < array.length; i++){
        soma = soma + array[i];
    }

    return soma;
}

function maior(array){
    if(array.length === 0){

    }

    let maior =  array[0];
    for(let i = 1)

}